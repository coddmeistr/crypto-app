database:
  docker: postgres://admin:root@postgres_container:5432/db
  local: postgres://postgres:maxgold123@localhost:5432/postgres

http:
  session_domain: account

server:
  port: :8080

env:
  mode: debug

validation:
  password:
    min_length: 6
    max_length: 20
  login:
    min_length: 6
    max_length: 20

dependencies:
  crypto_service:
    host: "http://localhost:8181"
    endpoints:
      current_prices: "/v1/crypto/prices"